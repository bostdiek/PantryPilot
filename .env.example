# PantryPilot Environment Variables Template
# Copy this file to .env.dev and .env.prod and fill in the actual values
# DO NOT commit .env.dev or .env.prod files to version control
#
# SECURITY NOTICE:
# - Never commit actual secrets or passwords to version control
# - Use strong, unique passwords for production environments
# - Generate cryptographically secure random strings for SECRET_KEY
# - Replace ALL placeholder values before deployment

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SECURITY: Use strong passwords with mixed case, numbers, and symbols
# Never use default or weak passwords in production
POSTGRES_USER=pantrypilot_user
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=pantrypilot
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Constructed database URL (used by FastAPI)
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# FASTAPI CONFIGURATION
# =============================================================================
# Environment: development, production, testing
ENVIRONMENT=development

# Security
# IMPORTANT: Generate a strong, random secret key for JWT signing
# Use at least 32 characters of random data (letters, numbers, symbols)
# Example generation: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_TO_LONG_RANDOM_STRING_FOR_JWT_SIGNING
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=PantryPilot
VERSION=0.1.0

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins
# Development example: http://localhost:5173,http://127.0.0.1:5173
# Production example: https://pantrypilot.com,https://www.pantrypilot.com
# IMPORTANT: Never use wildcards (*) when ALLOW_CREDENTIALS=true
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Backend API URL for frontend to connect to
VITE_API_URL=http://localhost:8000

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# Compose project name (affects container and volume names)
COMPOSE_PROJECT_NAME=pantrypilot

# =============================================================================
# PRODUCTION-SPECIFIC CONFIGURATION
# =============================================================================
# Domain configuration for production
DOMAIN=localhost
# Set to 1 to enable HTTPS/SSL
ENABLE_HTTPS=0

# Email configuration (for future notifications)
# SECURITY: Store email credentials securely, consider using app passwords
SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=CHANGE_ME_TO_EMAIL_APP_PASSWORD
SMTP_FROM_EMAIL=

# =============================================================================
# MONITORING AND LOGGING
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# =============================================================================
# SECURITY HEADERS (Production)
# =============================================================================
# Content Security Policy
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self' 'unsafe-inline'
CSP_STYLE_SRC='self' 'unsafe-inline'

# =============================================================================
# AI / LLM PROVIDERS
# =============================================================================
# Add your Gemini (Google) API key here if you plan to use the Gemini provider.
# Keep secrets out of version control and use environment-specific files (.env.dev,
# .env.prod) or secret stores in production.
# Example: GEMINI_API_KEY=ya29.a0AfH6SMD... (do NOT commit real keys)
GEMINI_API_KEY=

# =============================================================================
# AZURE COMMUNICATION SERVICES (Email)
# =============================================================================
# Connection string for Azure Communication Services
# Get this from Azure portal: Communication Services -> Keys
# Required for email verification and password reset functionality
AZURE_COMMUNICATION_CONNECTION_STRING=

# Verified sender email address in Azure Communication Services
# This must be a verified domain in your Azure Communication Services resource
# Example: DoNotReply@notify.pantrypilot.com
EMAIL_SENDER_ADDRESS=DoNotReply@notify.pantrypilot.com

# Frontend URL for email verification and password reset links
# Development: http://localhost:5173
# Production: https://pantrypilot.com
FRONTEND_URL=http://localhost:5173
