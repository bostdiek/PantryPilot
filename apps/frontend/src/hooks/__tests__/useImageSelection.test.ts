import { describe, it } from 'vitest';
// Deprecated duplicate test file retained only to avoid CI path references.
// Use `useImageSelection.test.tsx` for actual test coverage.
describe.skip('useImageSelection (deprecated .ts version)', () => {
  it('deprecated', () => {});
});
